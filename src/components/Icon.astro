---
interface Props { name: string; size?: number; class?: string; }
const { name, size = 24, class: cls } = Astro.props;
---
<svg width={size} height={size} class={cls} aria-hidden="true"><use href={`#icon-${name}`} /></svg>
